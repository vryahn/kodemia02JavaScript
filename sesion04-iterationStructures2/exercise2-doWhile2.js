// Ejercicio 2. Dado un número N imprime todos los valores iniciando desde 0 hasta un número antes del mencionado

const impNumbers = (num) => {
    let contador = 1;
    let suma = 0;
  	do {
      console.log(contador);
      contador++
    }  
  while(contador<num)
}

impNumbers(15);